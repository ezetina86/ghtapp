FROM node:20-alpine

WORKDIR /app

COPY package*.json ./

# Copy prisma directory before npm install so prisma generate can find the schema
COPY prisma ./prisma

RUN npm install

COPY . .

EXPOSE 5001

CMD ["npm", "run", "dev"]
